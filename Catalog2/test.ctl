$ruta = "C:\\Users\\Luis\\Music\\Musica";
print($ruta);

$var = "hola";
print($var);

foreach ($file inside $ruta){
	if ($file.Extension == "pdf"){
		print ($file.Name);
	}else{
		print ($file.Extension);
	}
}

